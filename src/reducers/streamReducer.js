import {
    DELETE_STREAM,
    EDIT_STREAM,
    CREATE_STREAM,
    FETCH_STREAM,
    FETCH_STREAMS
} from '../actions/types'

export default ( state = {}, action) => {
    switch(action.type){
        default:
            return state;
    }
};